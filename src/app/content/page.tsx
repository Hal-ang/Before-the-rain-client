import Header from "@/components/header/Header";
import React from "react";

const Content = () => {
  return (
    <main className="min-h-screen overflow-y-auto w-full flex ">
      <Header text="비가 오기 전에" />
    </main>
  );
};

export default Content;
